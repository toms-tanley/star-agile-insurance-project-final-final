pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                git 'https://github.com/toms-tanley/star-agile-insurance-project-final-final.git'
            }
        }
    }
}
